---
target: ทุกหน้าเว็บ (frontend/src)
total_score: 19
max_score: 40
na_heuristics: 
p0_count: 1
p1_count: 2
timestamp: 2026-08-13T04-16-29Z
slug: frontend-src
---
# Impeccable Critique — mtCenter (ทุกหน้า, frontend/src)

Method: dual-agent (A: design review · B: detector) · Mode: Operate · 2026-08-13

## Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 2 | สถานะจำนวนมากเป็นของปลอม ("Live Streaming" บนข้อมูล static, จุด ping บน mock); ชิปเครื่องใน TopBar เป็นสีเขียวเสมอแม้เครื่อง error |
| 2 | Match System / Real World | 3 | ศัพท์ช่างดีมาก (LOTO, MTTR, PM Tier-2) แต่ enum ดิบหลุดถึงผู้ใช้ ("high Priority", `{m.status}`) |
| 3 | User Control and Freedom | 1 | ไม่มี undo ที่ไหนเลย; อนุมัติ/สร้างใบงานทันทีไม่มียืนยัน; ยืนยันทุกอย่างด้วย `alert()` |
| 4 | Consistency and Standards | 2 | "ปกติ" = Medium ใน CreateWorkOrderView แต่ = Low ใน CreateWorkOrderModal; สี priority pill ต่างกันระหว่าง pillStyles.ts กับ modal |
| 5 | Error Prevention | 1 | อนุมัติเปิดสายการผลิตได้โดยไม่มี friction; AI auto-create ตั้ง priority=high อัตโนมัติ; ไม่มี dirty-form guard |
| 6 | Recognition Rather Than Recall | 3 | prompt chips/preset ดี แต่ฟอร์ม prefill เรื่องแต่ง (spindle bearing) ที่ผู้ใช้ต้องจำไปลบเอง |
| 7 | Flexibility and Efficiency | 2 | มี quick actions แต่ไม่มี sort/filter วันที่/bulk action ในทะเบียนใบงาน, ไม่มีคีย์ลัด |
| 8 | Aesthetic and Minimalist Design | 1 | อีโมจิ + Sparkles 14 ไฟล์ + ป้ายไทย(อังกฤษ)ซ้อนทุกหัวข้อ + จุด pulse ~10 จุด/หน้า |
| 9 | Error Recovery | 2 | offline fallback + toast ตรงไปตรงมา (ดี) แต่ AI drawer ปลอมแปลง error เป็นคำตอบแล้วพ่น raw error |
| 10 | Help and Documentation | 2 | HelpModal อธิบาย "ตัวเดโม" (role switcher) ไม่ได้อธิบาย workflow จริง |
| **Total** | | **19/40** | **Poor — ผิว swiss-polished แต่ interaction ระดับ prototype** |

## Design Specificity Verdict

**60% authored / 40% AI-template.** โครงเนื้อหาทำการบ้านมาจริง (ชื่อช่างไทย, รหัสอะไหล่ NSK, ขั้นตอน LOTO, ตำแหน่ง rack) แต่ผิวหน้าเต็มไปด้วยลายเซ็น AI: อีโมจิใน UI, Sparkles ระบาด 14 ไฟล์, ป้ายสองภาษาซ้อนทุกหัวข้อ, ตัวเลขปลอม ("98.4% Exceptional"), ชื่อ vendor หลุดถึงผู้ใช้ ("พร้อมใช้งาน • Gemini", "Gemini 3.6 Flash" ซึ่งเป็นรุ่นที่ไม่มีจริง)

**Deterministic scan:** 11 findings (detector รันแบบ degraded/regex — เป็น undercount) — bounce-easing ×5, side-tab ×3, border-accent-on-rounded ×2, gray-on-color ×1. ตรวจแล้วเป็น false positive 6 รายการ (มุม viewfinder ใน ScanMachineView:1122-1125, text-slate-950 บน amber ที่จริงถูกต้อง, จุด typing indicator ในแชท) ที่จริง: `animate-bounce` บน badge สถานะใน TopBar.tsx:117 และ SupervisorDashboardView.tsx:346, ลำแสงสแกนใน ScanMachineView:1082, `border-l-4` blockquote ใน ManualsView:60 — ทั้งหมดสอดคล้องกับข้อค้นพบ G7 ของรีวิว A

**Visual overlays:** ไม่ได้รัน — ไม่มี browser automation ใน session นี้

### จุด "เหมือน AI สร้าง" ระดับ global (ทุกหน้า)

- **G1 Sparkles ระบาด:** import ใน 14 ไฟล์ รวมถึง spinner อัปโหลดและหัวรายงาน — สัญญาณ AI-built อันดับ 1
- **G2 อีโมจิเป็นไอคอน UI** ในแอป safety-critical: 🚨⚡🌱💥🟢⚠️📍📦💡🤖✅❌ ในปุ่ม, `<option>`, หัวข้อ, toast
- **G3 ป้ายไทย (English) ซ้อนทุก string:** "ใบงานของฉัน (My Work Orders)" ฯลฯ — ผลิตภัณฑ์ไทยจริงเลือกภาษาเดียวต่อ string
- **G4 ชื่อ vendor/model ใน copy:** "Gemini 2.5 Flash", "Gemini 3.6 Flash", "Gemini PDF OCR Processed: Yes"
- **G5 ตัวเลขปลอมนำเสนอเป็นของจริง:** "98.4% Exceptional", "Live Streaming" บน array คงที่, "Sampling: 100Hz" บนกราф 6 จุด, aiVerificationScore default 92/95
- **G6 `alert()`/`prompt()` ทุก action สำคัญ:** logout, อนุมัติ, เบิกอะไหล่, export
- **G7 animation เร้าอารมณ์เกินเหตุ:** animate-ping บนจุด error, animate-bounce บน badge "ขัดข้อง", animate-pulse ~10 จุด/หน้า
- **G8 เรขาคณิตเหมือนกันหมด:** rounded-[18px] การ์ดขาวบน parchment + header สูตรเดียวทั้ง 12 หน้า
- **G9 token ชื่อ "Apple design system" ใน index.css** — ยืมตัวตนคนอื่น; ส่วน IBM Plex Sans Thai เป็นตัวเลือกที่ดีจริง

### รายหน้า (แก้อะไรในแต่ละหน้า)

- **ScanMachineView:** ~1,200 บรรทัด 7 section ซ้อน; serial ปลอมจาก string length; สเปก FANUC 31i-B แสดงทุกเครื่องรวม injection molder; dropdown "เลือกรหัสเครื่องที่จะจำลองสแกน" (UI จำลองหลุดมา prod) → ตัดเหลือ 3 บล็อก, สเปกมาจากข้อมูลเครื่องจริงหรือไม่แสดง, ลบ simulator
- **SupervisorDashboardView:** AI insight จาก setTimeout + `**markdown**` แตกเป็นตัวอักษรดิบ; ประวัติซ่อม 3 แถวเดิม (WO-2026-0881) ทุกเครื่อง → คำนวณ KPI จาก workOrders ที่ส่งเข้ามาแล้ว, render ผ่าน ReactMarkdown ที่มีอยู่แล้ว
- **MyWorkOrdersView:** pill "high Priority" (enum ดิบ); ปุ่ม 2 ปุ่มเรียก handler เดียวกัน; ไม่มี empty state เลย
- **AllWorkOrdersView:** prompt string เพี้ยน "ขอวิเคราะห์เชิงวิศวกรรม**สิทธิ์วิศวกร**/..."; ปุ่ม AI amber ทุกแถวดังกว่าปุ่มงานจริง; ไม่มี empty state ผลค้นหา; comment "Cards for Mobile" แต่ไม่ได้สร้างจริง (ตาราง 7 คอลัมน์บนมือถือ)
- **CreateWorkOrderView:** ฟอร์มใหม่ prefill งานซ่อมสมมุติ (4 ขั้นตอน + NSK 7014CTYN ×2); typo "ระบุชื่อสั้นสั้น"; field category เก็บแต่ไม่ใช้; WO code จาก Math.random()
- **PendingReviewView:** อนุมัติเปิดสายการผลิต = คลิกเดียว + alert; "AI Safety Score {…|| 95}%" — default คะแนนความปลอดภัยปลอม = บรรทัด copy อันตรายที่สุดในระบบ; "ส่งกลับแก้ไข" แค่เปิด modal เดิม
- **ManualsView:** banner โฆษณา "Gemini PDF Document Processing API" + ปุ่มเดโมฉีดคู่มือปลอมเข้า library; เนื้อคู่มืออวดตัวเอง "Extraction Quality: High Precision"
- **UploadManualView:** dropzone รับ drop ไม่ได้จริง (ไม่มี drag handlers); ประมวลผลปลอม setTimeout 2 วิ; ไม่มี error path
- **KnowledgeBaseView:** บทความ hardcode ในคอมโพเนนต์ เปิดอ่านไม่ได้ (ทำได้แค่ "ให้ AI สรุป"); นามสกุลผู้เขียนแบบ aptronym ("วิศวกรรม", "ชาญช่าง", "ไวไว")
- **SparePartsView:** รูป Unsplash stock รูปเดียวกันทั้ง bearing และ filter; จำนวนที่ยืนยันเบิกไม่ถูกอ่าน (uncontrolled input); confirm = alert
- **AIChatView / AIAssistantDrawer:** welcome hardcode "82.5°C" กับทุกเครื่อง warning; voice input ปลอม (พิมพ์ประโยค canned หลัง 2 วิ); error แต่งเป็น "ใช้คลังความรู้สำรอง" แล้วพ่น raw error; AI ตอบ `**bold**` เป็นตัวอักษรดิบ; ข้อดี: disclaimer "AI อาจให้ข้อมูลคลาดเคลื่อน..." เหมาะกับ domain มาก
- **SupervisorReportsView:** "98.4% Exceptional", "สำเร็จ 100%" hardcode; การ์ดเลือกประเภทรายงานเลือกแล้วไม่มีอะไรเปลี่ยน; export = setTimeout + alert
- **WorkOrderDetailModal:** ผู้รีวิว/ช่าง/คะแนน default เป็นค่าแต่ง; ย่อหน้า "Engineering Root Cause" เรื่อง spindle bearing โชว์ทุกใบงาน; technicianNote เก็บแล้วทิ้ง; **bug จริง: hooks ถูกเรียกหลัง early return** (rules-of-hooks violation)
- **Sidebar/TopBar:** ชิปเครื่องสีเขียวเสมอแม้ error (สัญญาณ glance สำคัญสุดของแอป); role switcher โชว์ใน production nav; notification hardcode + ลิงก์ "อ่านทั้งหมด" ตาย; class ขัดกัน `hidden md:flex`
- **GloveFriendlyCTA:** ปุ่มที่ copy อธิบายเหตุผลการออกแบบตัวเอง ("กดง่ายแม้ใส่ถุงมือ...") และ **ไม่เคยถูก render** — dead component
- **mockData.ts:** ไฟล์ที่ authored ที่สุด แต่ wo-105 ชื่อ CNC-01 ชี้ machineId m-01 (ซึ่งคือ #04)

## Overall Impression

โครงข้อมูลและ IA ตามบทบาท (ช่าง/วิศวกร/หัวหน้า) ดีจริง แต่ทุกครั้งที่ผู้ใช้แตะ action สำคัญจะเจอ alert(), ตัวเลขปลอม, หรือฟีเจอร์จำลอง โอกาสใหญ่สุด: เอาความจริง (ข้อมูล mock ที่คุณภาพดีอยู่แล้ว) ขึ้นมานำ แล้วถอดเครื่องสำอาง AI ออก

## What's Working

1. **เนื้อหา domain แท้:** รหัสอะไหล่, LOTO, ตาราง alarm code, พิกัด rack — เหนือกว่า template มาก เป็นฐานของความรู้สึก "authored"
2. **IA ตามบทบาท:** 3 บทบาทได้ nav + landing ต่างกันจริง ไม่ใช่ dashboard เดียวใช้ทุกคน
3. **AI ถูกถักเข้า workflow:** ทุกปุ่ม "ถาม AI" พก context เครื่อง/อุณหภูมิ/งานไปด้วย + disclaimer รับผิดชอบ

## Priority Issues

1. **[P0] อนุมัติ/auto-create ไม่มียืนยัน-undo + คะแนนความปลอดภัย AI ปลอม (`|| 95`, `|| 92`)** — แอปอ้างว่า gate สายการผลิต แตะพลาดครั้งเดียว = อนุมัติใบงานด้วยคะแนน compliance ที่แต่งขึ้น → แทน alert ด้วย confirm sheet สรุปใบงาน+คะแนนจริง (หรือ "ยังไม่ประเมิน"); auto-create ต้องลงที่ฟอร์มรีวิวเสมอ — `$impeccable harden`
2. **[P1] กวาดล้างอีโมจิ + Sparkles + ป้ายสองภาษา (ทุกหน้า)** — สัญญาณ "AI สร้าง" อันดับ 1 ตามที่ผู้ใช้ถาม → lucide icons เท่านั้น, ภาษาเดียวต่อ string (ไทยนำ อังกฤษเฉพาะศัพท์เทคนิค MTTR/LOTO), Sparkles เหลือ ≤2 จุด, เลิก ping/bounce บน status — `$impeccable quieter` + `$impeccable clarify`
3. **[P1] ฟีเจอร์จำลองที่ ship เป็นของจริง** — voice ปลอม ×2, QR simulator dropdown, ปุ่มเดโม PDF, export ปลอม, tab รายงานที่ไม่ทำงาน — จับโป๊ะครั้งเดียว ผู้ใช้เลิกเชื่อของจริงทั้งหมด → ลบ/gate หลัง `import.meta.env.DEV`/ติดป้าย "ตัวอย่าง (Demo)" — `$impeccable harden`
4. **[P2] สีสถานะขัดแย้งกันที่ชั้น glance** — TopBar เขียวเสมอ; "ปกติ"=Medium/Low คนละที่; palette pill ต่างกัน; enum ดิบ → token map เดียวใน pillStyles.ts ใช้ทุกที่ — `$impeccable polish`
5. **[P2] ไม่มี empty state + ฟอร์ม prefill เรื่องแต่ง** — grid ว่างดูเหมือนพัง; prefill สร้างใบงานผิดจริงได้ → empty state พร้อม next action; ฟอร์มเริ่มว่าง เดิม demote เป็น suggestion chips — `$impeccable onboard`

## Persona Red Flags

**Alex (หัวหน้างาน power user):** AI สรุปรายงาน = string canned + markdown แตก จับโป๊ะวันแรก; การ์ดประเภทรายงานเลือกแล้วเฉย; ทะเบียนใบงาน sort ไม่ได้ ไม่มี bulk ไม่มี pagination; ไม่มีคีย์ลัดใดๆ

**Sam (พึ่ง accessibility):** `select-none` ทั้งแอป (App.tsx:332) ก๊อปรหัสอะไหล่ไม่ได้/zoom-reader พัง; ป้าย text-[10px]/[11px] เล็กเกินสำหรับอักษรไทย; ปุ่มไอคอนล้วนไร้ aria-label (toast close, ปุ่ม Sparkles); checklist เป็น div คลิกได้ ไม่ใช่ checkbox — AT มองไม่เห็น; ไม่มี prefers-reduced-motion

**Casey (ใส่ถุงมือหน้างาน):** ปุ่ม +/− อะไหล่ w-5 h-5 (20px « 44px); "บันทึกผลการทำงาน" ห่างปุ่ม Sparkles 8px — มือถุงมือกดพลาดเปิด AI แทน; ScanMachineView ต้องเลื่อน ~5 จอถึงทางลัดช่าง; ตาราง 7 คอลัมน์บนมือถือไม่มี card fallback; ปุ่ม GloveFriendlyCTA ที่ออกแบบมาเพื่อเขาโดยเฉพาะ กลับไม่เคยถูก render

## Minor Observations

- Hooks หลัง early return ใน WorkOrderDetailModal — จะ crash เมื่อ isOpen สลับ
- font stack เอา -apple-system นำหน้า IBM Plex Sans Thai + `letter-spacing:-0.01em` ทั่วระบบ (อักษรไทยไม่ควร track ติดลบ)
- ตัวเลือกเครื่อง 3 ทางซ้ำกันบนจอเดียว (TopBar chip / pill row / QR modal)
- toast ไม่มี queue — อันใหม่ทับอันเก่า
- welcome effect ใน AIAssistantDrawer อ่าน stale closure — รหัสเครื่องค้างเมื่อสลับเครื่อง
- unused imports ~10 icon ใน ScanMachineView; Cpu ใช้ทั้ง "เครื่อง" และ "แรงดันไฮดรอลิก"

## Questions to Consider

1. ใครกด "อนุมัติ" ได้บ้าง — audit trail ของ "ใครเปิดสายการผลิต" ตอนนี้คือ alert() ที่ไม่มีใคร log
2. AI เป็นเพื่อนร่วมงานหรือ mascot? ถ้าลบ AI touchpoint เหลือ 2 จุด (drawer + action card) วันทำงานของช่างแย่ลงหรือดีขึ้น?
3. บ่ายเสาร์ server ล่ม + เครื่องรั่วน้ำมัน หน้าตาแอปควรเป็นยังไง? "ข้อมูล ณ 13:42" ซื่อสัตย์กว่าจุดเขียว pulse — ความซื่อสัตย์เรื่องความสดของข้อมูลคือสิ่งที่ template ไม่เคยทำ
